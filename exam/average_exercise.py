S = 'a,b,c,d,e,f,g,h,i,k'
#define a string containing 10 values
l = []
#define an empty list
for v in S:
	v = input('insert a value: ')
	L = l.insert(v)
#add string values to empty list one at a time
	if len(L) == 10:
#once 10 values inserted in list it defines a function called average
	def average(v):
	averageL = sum(L/2.0)
	return averageL, L


